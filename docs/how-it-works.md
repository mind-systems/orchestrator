[Back to README](../README.md) · [Режим рефакторинга →](refactor-mode.md)

# Как это работает

## Агентный конвейер

Каждый milestone проходит через две фазы, каждая из которых ограничена одним и тем же лимитом итераций.

Сначала идёт план-фаза: PlannerReviewer исследует кодовую базу и пишет план. PlanReviewer проверяет его и пишет результат в `.ai-factory/plan-reviews/` — либо с `PLAN_REVIEW_PASS` в конце, либо с описанием проблем. Если план не прошёл — Planner читает файл ревью и переписывает план. Цикл повторяется до `ORCHESTRATOR_MAX_ITERATIONS` раз. Если `PLAN_REVIEW_PASS` так и не появился — оркестратор останавливается: это значит Planner не может справиться с этим milestone.

Когда план утверждён, начинается implement-фаза: Implementer пишет код, PlannerReviewer ревьюит изменения и пишет результат в `.ai-factory/reviews/`. Если проблемы найдены — пишется патч, Implementer его применяет, ревью повторяется. Лимит тот же — `ORCHESTRATOR_MAX_ITERATIONS`. После последней итерации без `REVIEW_PASS` оркестратор останавливается с содержимым последнего ревью.

## Сессии агентов

Planner и Reviewer — один агент (`PlannerReviewer`), работающий в одной Claude-сессии через `--resume`. Это значит, что Reviewer знает весь контекст планирования — он понимает не только что было написано, но и почему. Implementer тоже держит сессию открытой между итерациями fix-цикла, накапливая контекст правок.

PlanReviewer — отдельный агент с чистой сессией на каждую попытку. Это намеренно: он смотрит на план без предвзятости автора, как независимый ревьюер.

## Файловый протокол

Агенты не общаются напрямую — только через файлы в `.ai-factory/` целевого проекта. Planner пишет план, Implementer его читает. PlanReviewer пишет ревью плана в `plan-reviews/`, Planner читает его при следующей попытке. Reviewer пишет ревью кода в `reviews/`, Implementer читает патч. Оркестратор координирует порядок вызовов и следит за сигналами (`PLAN_REVIEW_PASS`, `REVIEW_PASS`).

Между запусками оркестратор сохраняет состояние в `orchestrator-state.json`. В частности — список ревью-файлов, созданных в implement-фазе. Перед запуском review-прохода только эти файлы удаляются, остальные ревью (из предыдущих циклов) сохраняются.

## Сигналы завершения

Все агенты используют один и тот же протокол: пишут результат в файл, последней строкой ставят сигнал если всё хорошо — или оставляют файл без сигнала если проблемы остались. PlanReviewer пишет `PLAN_REVIEW_PASS`, Reviewer пишет `REVIEW_PASS`. Оркестратор читает файл, считает итерации и решает — продолжать, или остановиться и показать последнее ревью пользователю.

## See Also

- [Режим рефакторинга](refactor-mode.md) — как работает audit→plan→fix цепочка
- [Конфигурация](configuration.md) — лимиты итераций и модели агентов
