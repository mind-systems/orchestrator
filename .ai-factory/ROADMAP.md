# Project Roadmap

> Orchestrator — AI-driven multi-agent pipeline that autonomously plans, implements, and reviews code milestones.

## Hardening

- [x] **Caffeinate no-crash on non-macOS** — `_with_caffeinate` in `main.py` always spawns `caffeinate` via `Popen`, raising `FileNotFoundError` immediately on Linux. Wrap the `Popen` call with `try/except FileNotFoundError`: on failure, run the wrapped function directly without caffeinate and return the elapsed string. No behavior change on macOS. [3m 4s]

- [x] **Fix stderr pipe deadlock in `_run_claude`** — `stderr=subprocess.PIPE` combined with `proc.stderr.read()` called only after `proc.wait()` can deadlock if `claude` writes > ~64 KB to stderr before exiting: stderr pipe fills, subprocess blocks, our stdout loop stalls. Fix: replace `stderr=subprocess.PIPE` with `stderr=subprocess.STDOUT` to merge stderr into the stdout stream (already captured line by line). Remove the post-wait `proc.stderr.read()` call and the `stderr` variable from all downstream uses (`RuntimeError` messages). Touch `agents.py` only. [3m 14s]

- [x] **Planner Step 3 — kill dead Explore/Task sub-agents** — `orchestrator/prompts/planner.md` Step 3 ("Explore Codebase") tells the planner to launch 2-3 `Task(subagent_type: Explore)` agents, but the planner session's toolset is `["Read","Write","Glob","Grep","Bash"]` (`agents.py:249`) with no `Task` — the primary path is dead and every run silently takes the `Fallback: If Task tool is unavailable` branch, while Step 1 in the same file already does direct `Glob/Grep/Read`. Rewrite Step 3 to drill deeper with `Glob/Grep/Read` (architecture/modules, existing patterns, dependencies), delete the three `Task(...)` templates and the `Fallback:` line, and keep the synthesis bullets (files to create/modify, patterns, dependencies, risks). Touch `planner.md` only — do NOT modify `agents.py` or the sibling prompts (`test-planner.md`/`reviewer.md`/`implementer.md`), and leave the externally-passed plan path, `## Settings` hardcode, commit-prefix ban, and `done` rule intact. Spec: `.ai-factory/notes/02-planner-step3-direct-exploration.md`. [3m 21s]

- [x] **Prompts: follow mentions from the contract line** — Both agent prompts carry fixed checklists (`planner.md` Step 0: `DESCRIPTION`/`ARCHITECTURE`/`RULES`; `reviewer.md` Context Gates: `ARCHITECTURE`/`RULES`/`ROADMAP`) and nothing instructs going from the milestone's own line out along its links — the `Spec:` note is read only opportunistically; live incident: three review rounds issued semantic blockers without ever opening the phase's governing spec. Prompt-only fix, one rule instead of a document list: **lift the context tree from your contract line — follow mentions**. The `Spec:` tag is a mention; what the note mentions are next-level mentions; `Governing spec:` in the phase header is a mention seen when reading your line in the roadmap. Depth self-limits: only links reachable from your task — never a notes-directory sweep. `planner.md` + `test-planner.md` Step 0 gain the follow-mentions block; `reviewer.md` gains one matching gate (findings judged against the lifted tree, not the roadmap line alone; plan review holds only the plan path — recover the root by matching the plan's `# Plan:` title in `ROADMAP.md`/`ROADMAP_TESTS.md`, skip the gate if no line matches). No authority/ordering language; graceful no-op on single-tier roadmaps; no Python changes; `implementer.md` untouched. Spec: `.ai-factory/notes/03-follow-mentions.md`. [4m 30s]

- [x] **Prompts: drop DESCRIPTION.md reads** — DESCRIPTION.md is retired across the toolchain: it duplicated the target project's CLAUDE.md, which the claude CLI injects into every agent run unconditionally — the facts arrive for free. Delete the DESCRIPTION.md block from `planner.md` + `test-planner.md` Step 0 (`ARCHITECTURE.md`/`RULES.md` reads stay; do NOT add a "read CLAUDE.md" instruction — it's already in context); reword `planner.md` Step 1's skip condition ("Skip if DESCRIPTION.md provides sufficient context" → context already in hand); update `docs/target-project.md` (drop the DESCRIPTION requirement; ROADMAP + git required, ARCHITECTURE/RULES optional) and grep-sweep all docs. Prompt+docs only — `agents.py`/`main.py` expected untouched, verify by grep. Target-project DESCRIPTION.md files are not deleted — reads retire, files die naturally. Spec: `.ai-factory/notes/04-drop-description-reads.md`. [6m 22s]

- [x] **Prompts/docs: drop the sweep-guard clause; specs/ naming in docs** — The follow-mentions block carries a negative depth guard ("do not sweep the notes directory or read specs of unrelated tasks") — redundant over-protection: the positive rule (lift the tree from your contract line by mentions) self-limits depth **by construction**, and the name-bound prohibition already drifted (`notes/` → `specs/` lazy migration). Delete the guard clause from `planner.md`/`test-planner.md` and from `reviewer.md`'s gate, **replacing it with nothing** — the positive rule stays word-for-word. Separately, grep-sweep `docs/` (`context-model.md` "паутина нот", `target-project.md`) to state the pair where the directory is named as the specs' home: `specs/` current, `notes/` legacy served via the tags' literal paths. No Python (verify by grep); no migration instructions for target projects. Spec: `.ai-factory/notes/05-specs-wording.md`. [5m 29s]

- [x] **Auto-push after milestone commit** — `_git_commit` in `main.py` only committed; add `git push -u origin HEAD` right after the commit, warning and continuing (not raising) on push failure, so completed milestones reach the remote without a manual step.

- [x] **Re-review branch in `PlannerReviewer.review()`** — `review()` (`agents.py:283`) sends the same generic prompt on every pass while its session is persistent; live incident: the reviewer re-asserted an already-fixed finding as "unaddressed" for two passes and killed the run at max_iterations on a false failure. Mirror `plan()`'s pattern: add `prev_review_path`; when the session already exists, the prompt names the previous review and forces per-finding re-verification (re-read the cited file, quote current lines, verdict Fixed/Not fixed) before the normal full review. `main.py` passes iteration-1's review file. Session stays persistent — prompt-level fix; `PlanReviewer` untouched. Document the failure mode in `docs/non-convergence.md`. Spec: `.ai-factory/specs/01-reviewer-rereview-branch.md`. [4m 50s]

- [x] **Implementer feedback path — point at the real review file** — `Implementer.implement()` continuing-prompt (`agents.py:371`) says feedback is in `{patches_dir}`, but in implement mode `patches/` is never populated (reviews go to `reviews/`; only test mode bridges into `patches/`, `main.py:637`) — the implementer finds feedback only by its own initiative. Replace with an explicit `feedback_path` param: implement mode passes the failed review file (`reviews/{seq}-{slug}-review-{n}.md`), test mode passes the original test-run file from `test-runs/` directly — and retire `patches/` entirely: delete the copy-bridge (`main.py:637`), the dir creation in both modes, and the first-call `patches_note` scan. Resume behavior for both modes must not regress; docs sweep (`CLAUDE.md`, `docs/test-mode.md`, `docs/how-it-works.md`); target projects' leftover `patches/` dirs are not deleted. Spec: `.ai-factory/specs/02-implementer-feedback-path.md`. [12m 3s]

- [x] **`## Deferred observations` section in `reviewer.md`** — reviewers produce verified-but-deprioritized observations ("latent", "forward risk", "flagging for Phase N") as ad-hoc Notes that no one reads on success and `roadmap-prune` later deletes; no delivery channel to the downstream phases they address. Add a standardized `## Deferred observations` section to `prompts/reviewer.md` Output Format (entry: `- Affects: <phase/spec-note/"unknown"> — <observation>`), worded role-neutrally — one prompt serves both `PlanReviewer` and `PlannerReviewer`, so `plan-reviews/` and `reviews/` gain it from one edit. Critical: extend the PASS rules so deferred observations are not findings — a review with only them still passes; section sits before the signal line; no Python changes. Anti-laundering guard: deferral criterion is **scope, not severity** — anything introduced by the current diff or fixable within the milestone's boundary is a finding down to cosmetics; only fixes lying outside the milestone (other phase, not-yet-existing consumer) may be deferred. Status field after each entry is reserved for downstream tooling — the reviewer never writes or imitates processing marks. Harvest consumer lives in the skills repo (`roadmap-prune`), not here. Spec: `.ai-factory/specs/03-deferred-observations-section.md`. [9m 11s]

- [x] **Docs sweep + config-path literals** — Docs audit against current code: `docs/target-project.md` gains the auto-push behavior (`git push -u origin HEAD` after each milestone commit, warn-and-continue on failure); `docs/how-it-works.md` lists the full sidecar `step` set (adds `plan_review_failed:N` / `review_failed:N`); `CLAUDE.md` drops the wrong "(no review pass)" comment on the implement command. Code: error messages at `main.py:359,618` and docstrings in `config.py` said config lives at `~/.orchestrator.json` — corrected to `orchestrator.json` in the project root, matching actual load behavior (`config.py:24`).

- [x] **Total run time in terminal Telegram alerts** — Telegram is the only durable record of run time: console totals live in scrollback, and a rolled-back milestone (rescue → re-decompose) loses its sidecar `elapsed` entirely. Add module-level `run_started` to `state.py`, set it at the top of `run_implement()`/`run_test()`, and append `Ran for <elapsed>` as the final line of every **terminal** alert: all-milestones-done (`main.py:685`) and both stop handlers — `PipelineStopError` and `RateLimitError` (`main.py:780,787`). Guard: `None` timestamp → `"unknown"`, never crash a notify. Milestone alerts unchanged — per-milestone time is already durable in the roadmap and git history. No new alert types or config keys. Spec: `.ai-factory/specs/04-telegram-run-elapsed.md`. [4m 38s]

- [x] **Fail/success emoji prefix on Telegram alerts** — prepend 🟢 (success) or 🔴 (fail/stop) to every Telegram message so the outcome reads at a glance. Centralize in `notify()` (`notify.py`), keyed off `alert_type`: `_FAIL_ALERTS = {"stop"}` → 🔴, everything else (`milestone`, `done`) → 🟢. Call sites unchanged.

- [x] **Tests-first: alert colour mapping + `cli()` exception routing** — no test currently touches `notify`, the emoji prefix, or the `cli()` exception handlers, yet these are silent-failure surfaces (wrong colour / mis-routed exception = wrong signal, no crash). Write them before task 05: new `tests/test_notify.py` pins the pure mapping `stop`→🔴 / `halt`→🟡 / `milestone`,`done`→🟢 (monkeypatch `send_telegram`, never hit the network) and the `telegram_alerts` gating; additions to `tests/test_main.py` pin `cli()` routing (`PipelineStopError`→`stop`, `HaltError`/`RateLimitError`→`halt`, generic `Exception`→`halt` **and re-raised**) and that `_check_usage_limits` / resume-past-max raise `HaltError`. Assertions on the new behaviour are red until 05 lands — by design. Skip loud/non-deterministic surfaces (network, agents, git, signals). Spec: `.ai-factory/specs/07-notify-routing-tests.md`. [11m 11s]

- [x] **Yellow `halt` alert — red only when the milestone could not be completed** — colour by cause, not exception class. `stop` (🔴) stays only where a milestone fails to complete (plan/code/test loop exhausts `max_iterations` without PASS, or the checkbox never gets marked). Everything else is a 🟡 `halt`: usage-threshold breach (`main.py:63,67`) and resume-past-max (`main.py:357,622`) — currently `PipelineStopError`, must move to a new `HaltError`; `RateLimitError` (`agents.py`) becomes a `HaltError` subclass; single- and double-Ctrl+C manual stop (`main.py:714`, `_handle_sigint`); any other exception reaching `cli()` (`except Exception` → alert `halt`, then re-raise so the traceback survives). `cli()` gains `except HaltError` (🟡) beside the unchanged `except PipelineStopError` (🔴); `notify.py` gains `_HALT_ALERTS` (three-way emoji pick); force-quit reads a new `state.config`/`state.project_dir` stashed at the top of `run_implement`/`run_test`. Code-only — docs already describe the target taxonomy. Spec: `.ai-factory/specs/05-stop-halt-yellow-signal.md`. [10m 47s]

- [x] **Tests-first: complete the resume-dispatch matrix for both step-detectors** — the step-detector (`sidecar step + disk → (step, counter, plan_path)`) is the only unit-testable core of the pipeline and the sole safety net for the task-06 dedup (the processor loop drives agents/subprocess — untestable). `tests/test_main.py` already covers `_validate_sidecar_step` and some detector branches, but the matrix is incomplete — notably the implement-mode `done` path, `plan_reviewed`/`plan_review_failed` dispatch, and the heuristic fall-through branches, in both `_detect_milestone_step` and `_detect_test_milestone_step`. Fill every uncovered return branch as green characterization over current behaviour (reuse the existing `_dms_dirs`/`_dtms_dirs` fixtures; don't restructure existing tests). All green now; task 06 must keep them green. Spec: `.ai-factory/specs/08-detector-matrix-tests.md`. [10m 25s]

- [x] **Express the milestone pipeline once (implement/test)** — `process_milestone` (154 lines) and `process_test_milestone` (146) are ~95% duplicates, and the same duplication runs through their step-detectors (`_detect_milestone_step`/`_detect_test_milestone_step`; `_validate_sidecar_step` is already shared) — the twin homes for the raise sites, step vocabulary, and resume logic that must be kept in lockstep by hand. Collapse each pair into one function parametrized by a small `mode` descriptor carrying the only real divergences: roadmap file, header label, planner prompt, output dir + artifact suffix, step-name vocab (`review`/`test_run`), pass-signal string (`REVIEW_PASS`/`TEST_PASS`), print/raise labels, and a verify closure (`PlannerReviewer.review` with prev-review re-check vs stateless `TestRunner.run`). Pure behaviour-preserving refactor — resume from every `step` in both modes must dispatch identically; preserve implement-only `prev_review_path` re-verification and task-05's `HaltError`/`PipelineStopError` split. Depends on 05. The follow-up module extraction is its own task below. Touch `main.py` only. Spec: `.ai-factory/specs/06-unify-milestone-pipeline.md`. [16m 0s]

- [x] **Extract main.py helpers into cohesive modules** — `main.py` mixes the pipeline with unrelated concerns; size is a symptom, the real smell is one module owning many responsibilities (600 lines is no better than 800). Move three cohesive clusters out, pure and behaviour-preserving: usage gating (`_parse_pct`, `_check_usage_limits`, SESSION/WEEKLY patterns) → `usage.py`; resume/step-detection (`_validate_sidecar_step` + the unified detector) → `resume.py`; run lifecycle (`_handle_sigint`, `_with_caffeinate`, `_fmt_elapsed`, `_run_elapsed`) → `runtime.py`. `main.py` keeps the pipeline, loops, `_git_commit`, `run_*`, `cli`. Runs **last** — after 05 (touches `_handle_sigint`) and 06 (unifies the detector) — so it moves settled code with no re-churn. Updates the moved tests' imports (`test_main` → `orchestrator.usage`/`orchestrator.resume`; Class-A drift only); `uv run pytest` green before and after, zero behaviour change. Spec: `.ai-factory/specs/09-extract-main-modules.md`. [10m 47s]

- [x] **Rename alert types: `stop`→`milestone-fail`, `halt`→`stop`** — `stop` (🔴 failure) and `halt` (🟡 operational stop) are near-synonyms whose names don't say which is which — the ambiguity misled planning once. Rename for self-documenting names: red `stop`→`milestone-fail`, yellow `halt`→`stop` (pairs with `milestone`/`done`). Pure rename sweep, runs **last** after 05/07 settle: `notify.py` set values, every `cli()`/manual `notify(...)` call, the assertions in `tests/test_notify.py` + `tests/test_main.py`, and the `docs/configuration.md` alert-types table + example (concept doc names no strings — leave it). **Breaking config change:** the literal `stop` flips meaning (🔴→🟡), no auto-migration — document the meaning-flip so `["stop", …]` configs are updated consciously. Also enumerate every valid value in `orchestrator.json.example` via a `telegram_alerts_example_all` sibling key (`["milestone-fail","stop","milestone","done"]`) — it ships `[]` today and hides what's accepted. No behaviour change otherwise. Spec: `.ai-factory/specs/10-rename-alert-types.md`. [11m 17s]

- [x] **Prompts: one philosophy pass — load the written philosophy, roll it over every agent prompt** — the four prompts partially embody the philosophy the skills repo has since written down (both planners build the milestone's **context tree**, `planner.md:21-25`/`test-planner.md:18`; the reviewer judges findings against it), with two gaps: the planners' tree stops at **two hops** (a note referencing a note that names a code file is never followed to the end), and the implementer has **no discipline for a wrong or underspecified plan** (`grep "escalat\|contradict\|deviate"` → zero). One pass with a hard read-first mandate — `skills/docs/context-tree.md`, `skills/docs/skill-composition-model.md`, `skills/src/global/CLAUDE.md` § Grounding claims — then: planners' mirrored Follow-mentions block → recursion **down to the leaf; the leaf is code** (width limiter and `Governing spec:` read survive verbatim); implementer → **ground truth wins**, `DEVIATION: <plan said / file showed / done>` on the task's plan-file line instead of silent deviation, `BLOCKED: <missing decision>` instead of plausible invention (annotations ride the plan file — headless stays headless); reviewer → audit only, conformance expected. Prompts only, no Python; pass-signal and `done`-output contracts untouched. Spec: `.ai-factory/specs/11-prompts-philosophy-pass.md`. [11m 39s]

## Multiuser roadmaps

Per-developer named roadmaps — the orchestrator's half of the multiuser design. Governing spec: `~/projects/skills/docs/multiuser-roadmaps.md` (on conflict it wins); origin handoff: `.ai-factory/handoffs/03-multiuser-named-roadmaps-orchestrator-side.md`. Hard acceptance: absence of the new setting is byte-stable — a project without `.ai-factory/roadmaps/` sees zero behavior change.

- [x] **Roadmap-path setting — named roadmaps through the `roadmap_filename` seam** — the loops hardcode the default pair: `_implement_loop` defaults `roadmap_filename="ROADMAP.md"` (`main.py:344`), `_test_loop` hardcodes `ROADMAP_TESTS.md` (`main.py:333`), and `cli()` overrides neither. Add optional `roadmap_path` to `orchestrator.json` with three explicit states (no null-implies-derivation): absent → `ROADMAP.md` (byte-stable); literal `my` → family derivation (slug from `git config user.email` local-part, fallback `user.name`) at `roadmaps/<slug>.md` with the `> Owner:` line verified — mismatch = `HaltError`, file missing → loud fallback to `ROADMAP.md` (lazy migration: one workstation setting everywhere); any other value → explicit relative path under `.ai-factory/`, no owner check (explicit wins; absolute or `..` → loud exit). Widen the seam filename → relative path (rename `roadmap_filename`→`roadmap_relpath`), derive the test sibling from the roadmap in play (`<name>-tests.md`; default pair stays the special case), widen `reviewer.md`'s two roadmap-name hardcodes. Unit tests pin slug derivation, owner gate, three-state resolution, sibling, config guards. Spec: `.ai-factory/specs/12-roadmap-path-setting.md`. [15m 55s]

- [x] **Per-roadmap artifact subdirectories — users never share a number axis** — artifacts are committed with each milestone (`git add -A`, `main.py:96`) and spread through merges, so flat `plans/`/`plan-reviews/`/`reviews/`/`test-runs/` become the union of every developer's queues: concurrent runs mint duplicate `NN` (`_next_number`, `main.py:82`), merged foreign same-slug plans feed the resume glob, same `NN`+slug is a real merge conflict — the governing spec's "artifacts stay flat" premise ("branch-local") is factually wrong and is amended alongside. After task 12: pure `_artifact_subdir(relpath)` — default pair → `None` (flat, byte-stable), anything else → the roadmap file's stem (`roadmaps/kg-wmservice.md` → `plans/kg-wmservice/…`); thread as a `Mode` field via the existing `_replace` setup; `_next_number`/`_detect_step` receive subdir'd paths through existing params — `resume.py` and the numbering mechanism untouched. Protocol mirrors (`orchestrator-artifacts`, prune sweep) are skills-repo tasks. Unit tests pin the key mapping. Spec: `.ai-factory/specs/13-artifact-subdirs.md`. [9m 19s]

- [x] **Terminal alert format: uniform fail lines + per-run done counter** — one concern, what a terminal Telegram alert says. (1) The three fail raise sites word their first lines differently, and only the first line reaches Telegram (`cli()` takes `splitlines()[0]`, `main.py:406`); unify to exactly `Plan failed` / `Implement failed` / `Test failed` (`main.py:207,53,69`) — no attempt counts; the body (`Last review: {path}` + content) stays for console and rescue; invariant raises (`main.py:222,309`) keep their wording. (2) Every terminal alert gains the run's completed count: `state.milestones_done` reset per run, incremented after `mark_done` in both completion paths (`main.py:149,266`; `mark_skipped` not counted), new `runtime._run_summary()` → `Ran for {elapsed} · {n} milestones done` replaces the `Ran for` fragment at all six terminal sites (`main.py:303,328,407,414,419`; force-quit `runtime.py:20`). Per-milestone green alerts untouched; counter is per-run, never roadmap progress. Spec: `.ai-factory/specs/15-terminal-alert-format.md`. [9m 36s]

- [ ] **Resume adoption gate — adopt in-flight artifacts only** — `_detect_step` (`resume.py:76-89`) adopts the lowest-numbered `*-{slug}.md` plan unconditionally; slug is the slugified title, so a recurring milestone title matches a long-completed plan and its `done` sidecar → `mark_done` + commit with zero work (live single-user incident; title checks can't discriminate — same slug ⟺ same title). Gate by the `orchestrator-artifacts` protocol line "tracked artifacts belong to completed tasks, uncommitted ones to failed/in-flight": candidate plan tracked+clean (`git status --porcelain` empty for it) → stale, skip; adopt the lowest-numbered in-flight survivor; none → fresh plan. Staged-but-uncommitted (the `git add -A` at review, `main.py:245`) stays adoptable — the crash-before-`mark_done` path survives. Git-status errors fail open toward re-planning, never toward `done`. Tests over tmp git repos: committed-skip / untracked-adopt / staged-adopt / survivor-over-lowest; specs-08 matrix stays green. Spec: `.ai-factory/specs/14-resume-adoption-gate.md`.

---STOP---

- [ ] **Interactive REPL mode** — Bare `uv run orchestrator` (no subcommand) currently defaults to implement on current dir. Replace with an interactive session: load config from `~/.orchestrator.json`, display settings on entry, prompt `>` with readline history. Commands: `implement <path>`, `test <path>`, `set <key> <value>` (type-validated, in-session only), `show`, `save` (atomic write to config file via tmp+replace), `help`, `exit`. Ctrl+C during pipeline → stop run, return to prompt. New `orchestrator/repl.py`; `cli()` routes `args.command is None` to `run_repl(config, config_path)`; `load_config()` return type changes to `tuple[OrchestratorConfig, Path]`. Zero new dependencies — `readline` (stdlib). Update `CLAUDE.md` Commands section; mention REPL in `docs/workflow.md`. Spec: `.ai-factory/notes/01-repl.md`.
