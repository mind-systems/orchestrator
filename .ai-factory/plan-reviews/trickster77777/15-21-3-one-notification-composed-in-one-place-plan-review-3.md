# Plan Review: 21.3 — One notification, composed in one place (round 3)

## Code Review Summary

**Files Reviewed:** plan + 6 target files (`orchestrator/notify.py`, `orchestrator/main.py`, `orchestrator/runtime.py`, `tests/test_notify.py`, `tests/test_runtime.py`, `tests/test_main.py`) + spec `54-one-notification-shape.md` + contract line `roadmaps/trickster77777.md:115` + governing spec `docs/concepts/fault-handling.md` + `.ai-factory/ARCHITECTURE.md`
**Risk Level:** 🟢 Low — round 2's single finding is closed, and no new defect survives verification. Every coordinate, token, word, and API claim in the plan was re-checked against the files as they stand today; the suite is green at 211 passed before the change, so the breakage this task causes is the breakage the spec predicts.

### Context Gates

- **Architecture (`.ai-factory/ARCHITECTURE.md`) — OK.** `:46` permits `main`→`notify`; `:48` lists `runtime`→`notify` among the downward support imports. That is exactly why `run_summary` must stay a parameter of `report()` (Task 3): `_run_summary()` is defined natively in `runtime.py:38`, so `notify.py` reaching for it would close a cycle and break `:48`. The plan states this reasoning at the point an implementer would be tempted to "simplify" it away. The module-tree comment at `:28` ("Support: Telegram alerts") and the layer sentence at `:61` stay accurate with a composer added — composing the alert's text is alert work, not a new concern. The `## Features` table (`:123`) maps features to commit hashes and is maintained by `roadmap-prune`, not by a task plan, so its "Telegram alerts — colour-coded outcomes" row is correctly left alone.
- **Rules — WARN (file absent).** No `.ai-factory/RULES.md` in this repo (`.ai-factory/` holds `ARCHITECTURE.md`, `handoffs`, `notes`, `orchestrator.json`, `plan-reviews`, `plans`, `reviews`, `roadmaps`, `specs`, `test-runs`). No explicit convention file to check against.
- **Roadmap — OK.** `roadmaps/trickster77777.md:115` is the task; its `Spec:` tag resolves to `.ai-factory/specs/trickster77777/54-one-notification-shape.md`, whose governing spec is named on the Phase 21 header paragraph: `docs/concepts/fault-handling.md`. The plan implements that tree faithfully — `:72` ("a notification is a signal, not the account… its detail is therefore a short fixed label or nothing at all: never a filesystem path, never a line an agent wrote"), `:74`/`:83` (colour says work vs. machine — preserved because `_ALERT_TYPES` reproduces today's tokens verbatim), `:85` ("one shape, composed in one place"), and invariants 5 and 6 at `:93-94`. `Docs: no` is correct: the concept page is already written in the present tense ahead of the code, and no doc quotes a per-site message string — `docs/reference/configuration.md:77-89` pins tokens and colours only (both untouched), `docs/features/escalation.md:25` and `docs/concepts/outcomes.md` link to that table rather than copying it, and `docs/future/run-context-refactor.md`'s mentions of "the force-quit notification" describe a notification that still exists.
- **Plan-layer citation rule (Phase 19) — OK.** § Context labels `roadmaps/trickster77777.md:87` as the header and `:89` as the opening paragraph — verified, and the quoted sentence ("A later task that rewrites one of those lines for its own reasons takes the coordinate with it; a line no task touches keeps its prefix until the cross-repo ask is answered") is verbatim. The prefix census is verifiable and matches: `test_main.py` 14, `test_runtime.py` 5, `test_notify.py` 2 = 21. Exactly one of those lines is rewritten by this task (`test_runtime.py:173-174`, whose second line reads `# notify guard`), and only it loses its coordinate. No `# Task N:` header sits anywhere in the `test_main.py` region this task edits — the nearest are `:791` above and none until past `:940`; the section comment over the `cli()` routing tests is the coordinate-free `# cli() exception-routing tests`, as the plan says. No sweep, which 19.1 forbids.

### Round-2 finding — closed

**(MEDIUM) The positive `test_runtime.py` test kept `notifies` in its name.** Closed. Task 6 now enumerates `:178` alongside its siblings — `test_handle_sigint_second_press_notifies_when_config_and_project_dir_set` → `..._reports_when_config_and_project_dir_set` — and states the reason in the plan's own words ("the standing rule has no survivor, and one behaviour must not be described with two verbs across three adjacent tests"). With `:207` and `:230` renamed to `..._no_report_when_...`, the docstring at `:179`, the two "WITHOUT notifying" phrases, the three `notify_mock` locals, and the section comment at `:173-174` all repointed, the triple now names one verb and no identifier in either test file survives naming a call `runtime.py` can no longer make. Round 1's three findings remain closed.

### Verified against the codebase (no defect found)

- **The eight `main.py` sites and the one in `runtime.py` are the complete set.** A repo-wide grep for `notify` over `*.py` returns exactly: the definition (`notify.py:24`), the two imports (`main.py:16`, `runtime.py:11`), the eight calls (`:244`, `:363`, `:398`, `:423`, `:509`, `:516`, `:523`, `:526-530`), `runtime.py:20`, and the test references. Nothing else calls or patches it.
- **Every token the plan pins to `_ALERT_TYPES` matches the live call site**, one by one: `:244`/`:363`→`"task"`, `:398`→`"done"`, `:423`→`"stop"`, `:509`→`"task-fail"`, `:516`→`"stop"`, `:523`→`"escalation"`, `:526-530`→`"stop"`, `runtime.py:20`→`"stop"`. A `telegram_alerts` list written before this task therefore keeps selecting exactly the alerts it always did — the contract line's own guard.
- **The eight `Outcome` members, their values, and their eight words** reproduce the spec's §1 and §2 tables exactly, and the words are pairwise distinct — `Unconverged` and `Stopped` no longer share, which is defect #2 closed.
- `msg = str(e).splitlines()[0]` sits at `:508`, `:515`, `:522` as stated, and the `ERRORED` expression the plan pins matches `:528` character-for-character, `if str(e) else ''` included.
- **Deleting `msg` in the `EscalationError` arm is safe** — `print(f"ESCALATED — {e}")` (`:520`) reads `e`, not `msg`, and `msg` has no other use in that arm. The account is not lost with the alert's detail: it stays in the run's stdout and in the artifact, which is precisely what `fault-handling.md:72` prescribes.
- **Exception-arm ordering holds for the new fourth test.** `EscalationError` is a direct `Exception` subclass (`agents.py:168`), not a sibling of the two arms above it (`except PipelineStopError` at `:504`, `except HaltError` at `:511`), so `except EscalationError` at `:518` is the arm that fires; and `RateLimitError(HaltError)` (`agents.py:154`) does route the `:922` case to `HALTED`, as the plan's parenthetical claims.
- `config`, `project_dir`, and `_run_summary` (imported at `main.py:19`) are in scope at all eight sites; `runtime.py:20`'s enclosing `if state.config is not None and state.project_dir is not None:` guard is where the plan says.
- `notify.py` carries `from __future__ import annotations` (`:3`) and a `TYPE_CHECKING` import of `OrchestratorConfig` (`:10-11`), so `detail: str | None`, `config: "OrchestratorConfig"`, and the module-level `dict[Outcome, str]` annotations all hold. `enum` is stdlib, so the docstring's "stdlib only, no new dependencies" stays true.
- **Patch targets hold.** `from .notify import Outcome, report` binds `report` into both namespaces, keeping `main_module.report` / `runtime.report` monkeypatchable; `report()` calling the module-global `notify` keeps `notify_module.notify` patchable for Task 7's last case, and importing `report` by name into the test does not defeat that — the global is resolved at call time.
- Test coordinates: `test_runtime.py:173-174` comment, `:178`/`:207`/`:230` names, `:185`/`:214`/`:237` patch lines, and the `Mock(name="project_dir")` + `.name = "myproject"` fixture that makes `call_args[0][2] == "myproject"` hold after the rewrite; `test_main.py:893-909` `_run_cli_with` with its docstring at `:894` and its `main_module.notify` patch at `:907`, its three consumers at `:912-933`, the import block at `:10-22` that never imports `.notify`, and `EscalationError` already imported at `:13`. `_run_cli_with` has exactly those three consumers, so retyping the recorded tuple breaks nothing else. `test_notify.py` is 107 lines, the `sent`-fixture cases run `:22-107`, `notify_module` is imported at `:5`, and the two `# Task N:` headers sit at `:35` and `:85` — all as the plan states.
- **Task 8's greps will behave as stated.** No pre-existing `report(` substring exists in either module to inflate the count, so the change yields zero `notify(` hits (the new import line carries no parenthesis) and exactly nine `report(` hits, eight plus one.
- **Baseline:** `uv run pytest` is green today (211 passed), and the working tree carries no code changes — only planning artifacts. So the tests Task 6 repoints are red only because this task makes them so, exactly as the spec's § Tests predicts.

### Critical Issues

None. Nothing in the plan misstates the codebase, misuses an API, skips a step the spec mandates, or reaches outside the touch-list. `agents.py` stays untouched, `EscalationError` stays single-argument, and the resumed-escalation raise at `:237` correctly needs no edit — the spec names it inside the *permitted* boundary, not as required work, and the `ESCALATED` site reads nothing off the exception.

### Positive Notes

- Task 6's fourth case is the plan's load-bearing addition and the only assertion anywhere that `main.py:522`'s site passes no detail. The plan says exactly why it exists — a future edit restoring `msg = str(e).splitlines()[0]` there would otherwise put the path and the model prose back into the alert with a green suite. That is defect #3 pinned, not merely fixed.
- § Context is written as a list of refusals with reasons — no `_cap()` helper, no `agents.py` edit, no `:237` edit, no coordinate sweep — which is what keeps a plan of eight small tasks from growing a ninth mid-implementation.
- Task 4 pins the `ERRORED` detail character-for-character and pairs the `EscalationError` deletion with its safety argument, so the two sites whose text is genuinely load-bearing cannot drift during an otherwise mechanical rewrite.
- The dependency reasoning for `run_summary` as a parameter is placed where the temptation to inline it would arise, and it matches `ARCHITECTURE.md:48` rather than merely asserting a preference.
- Task 7 spends one assertion on the whole `_WORDS` mapping (`set(_WORDS) == set(Outcome)` plus a distinct-values count) instead of eight near-identical cases, and puts token routing on `_ALERT_TYPES` directly rather than leaving it implied by a site test.
- Task 8 turns the spec's § Verify into two greps with expected hit counts plus one read-level trace, so "no call site names a token" is falsifiable rather than asserted.

## Deferred observations

- Affects: `.ai-factory/specs/trickster77777/54-one-notification-shape.md` § 4 / `docs/concepts/fault-handling.md:72` (invariant 5) — the spec keeps `str(e).splitlines()[0]` for `UNCONVERGED` and `HALTED` on the grounds that those messages "open with a fixed summary clause and continue into a path or raw output". Two live raise sites do not fit that description: `RateLimitError(result_text)` (`agents.py:350`) makes the whole exception message the agent's own result text, whose first line is a line an agent wrote, and `_resolve_roadmap_relpath`'s owner-mismatch `HaltError` (`main.py:152-155`) is a single sentence carrying a roadmap path and a quoted file line. Both are `HaltError`, so both reach the `HALTED` detail intact and the alert keeps carrying a path or agent prose — what invariant 5 forbids. Closing it means either bounding the detail by length in `compose()`/`report()` or reshaping those two raise sites, and both contradict this task's ratified pins (`agents.py` is explicitly out of the touch-list; § 4 pins the line cut "exactly as today"), so it belongs to a later task in Phase 21, not to this plan.

PLAN_REVIEW_PASS
